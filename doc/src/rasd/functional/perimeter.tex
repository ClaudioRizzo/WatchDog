\subsubsection{Perimeter selection}
\small{The perimeter selection feature, when activeted, must select a circle area centered where the cellphone is at that momente, and trigger an alarm if the mobile phone exits from there. The circular limit has also to be visible on a map on the telefone%, along with the actual position inside it%.}